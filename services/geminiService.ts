
// AI services removed as requested.
export const generatePlaceDescription = async (placeName: string, category: string): Promise<string> => {
  return "";
};

export const suggestTags = async (description: string): Promise<string[]> => {
  return [];
};
